#!/bin/bash

FOLDER="~/Pictures/screenshots"

mkdir -p $FOLDER

maim -s ~/Pictures/screenshots/$(date +%s).png && \
  FILE=$(ls -1 Pictures/screenshots --sort time | head -n 1) && \
  notify-send -t 2500 "Screenshot $($FILE) saved."
